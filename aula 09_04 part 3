#include <stdio.h>

int main() {
  float nota;
  char *resultado;
  
  printf("digite a nota: ");
  scanf("%f", &nota);
  
  resultado = nota >= 6 ? (nota == 10 ? "Aprovado com nota maxima" : "Aprovado") :
"reprovado";
    
 printf("%s\n", resultado);
 
 return 0;
}
